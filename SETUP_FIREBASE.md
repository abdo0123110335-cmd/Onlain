# ربط التطبيق بـ Firebase — دليل خطوة بخطوة

اتبع الخطوات دي بالترتيب مرة واحدة بس. بعدها التطبيق هيبني نفسه تلقائياً مع كل تحديث ترفعه على GitHub.

---

## 1) إنشاء مشروع Firebase

1. روح إلى: https://console.firebase.google.com
2. اضغط **Add project / إضافة مشروع**.
3. اختر اسم للمشروع (مثلاً: `elsheikh-customs`) واستمر بالخطوات الافتراضية حتى ينتهي الإنشاء.

## 2) تفعيل تسجيل الدخول بالبريد وكلمة المرور

1. من القائمة الجانبية: **Build → Authentication**.
2. اضغط **Get started**.
3. من تبويب **Sign-in method**، فعّل **Email/Password**.

## 3) إنشاء قاعدة بيانات Firestore

1. من القائمة الجانبية: **Build → Firestore Database**.
2. اضغط **Create database**، واختر أي موقع سيرفر قريب (مثلاً `eur3` أو `nam5`)، وابدأ في **وضع الإنتاج (Production mode)**.
3. بعد الإنشاء، افتح تبويب **Rules** والصق فيه محتوى ملف `firestore.rules` الموجود في هذا المستودع (استبدل القواعد الافتراضية بالكامل)، ثم اضغط **Publish**.

## 4) تفعيل Firebase Storage (لحفظ صور المستندات)

1. من القائمة الجانبية: **Build → Storage**.
2. اضغط **Get started** واستمر بالإعدادات الافتراضية.
3. من تبويب **Rules**، الصق محتوى ملف `storage.rules` من هذا المستودع، ثم **Publish**.

## 5) إضافة تطبيق أندرويد للمشروع

1. من الصفحة الرئيسية للمشروع، اضغط أيقونة أندرويد ➕ لإضافة تطبيق.
2. في خانة **Android package name** اكتب بالضبط:
   ```
   com.elsheikhcustoms.elsheikh_customs_app
   ```
   ⚠️ هذا الاسم يجب أن يُكتب حرفياً كما هو، لأن خط البناء (CI) يستخدم نفس الاسم بالضبط.
3. اسم التطبيق (Nickname) اختياري، اكتب مثلاً "أعمال الشيخ مختار".
4. اضغط **Register app**.
5. في الخطوة التالية سيعرض لك زر **Download google-services.json** — نزّله واحتفظ به (هتحتاج محتواه في الخطوة الجاية). لا داعي لأي خطوات أخرى بعدها (لا تحتاج تضيف أي كود يدوياً) — اضغط **Next** حتى النهاية أو أغلق النافذة.

## 6) إضافة ملف الإعداد كـ Secret في GitHub

1. افتح ملف `google-services.json` اللي نزّلته بأي محرر نصوص، وانسخ **محتواه بالكامل**.
2. روح لمستودعك على GitHub → **Settings → Secrets and variables → Actions**.
3. اضغط **New repository secret**.
4. الاسم (Name): بالضبط
   ```
   GOOGLE_SERVICES_JSON
   ```
5. القيمة (Value/Secret): الصق محتوى الملف كامل كما هو.
6. اضغط **Add secret**.

## 7) تشغيل البناء

1. أي تعديل تعمله وترفعه (push) لفرع `main` سيشغّل البناء تلقائياً.
2. أو من تبويب **Actions** في المستودع، اختر الـ workflow، واضغط **Run workflow** يدوياً.
3. بعد انتهاء البناء بنجاح، حمّل ملف الـ APK من قسم **Artifacts** بنفس صفحة التشغيل، ووزّعه على تلفونات الموظفين والمدير.

---

## 8) إنشاء أول حساب "مدير"

النظام يجعل **أول شخص يسجّل دخول بنجاح** مديراً تلقائياً — لكن الحساب نفسه (يوزر + باسورد) لازم يُنشأ يدوياً مرة واحدة من كونسول Firebase:

1. من كونسول Firebase: **Authentication → Users → Add user**.
2. مهم جداً: التطبيق يتعامل مع "اسم المستخدم" بإضافة نطاق ثابت خلف الكواليس. يعني لو عايز اسم مستخدم للمدير يكون مثلاً `admin`، فلازم تكتب في خانة الإيميل هنا بالضبط:
   ```
   admin@elsheikh.customsapp
   ```
3. اكتب كلمة مرور (6 أحرف فأكثر) واضغط **Add user**.
4. افتح التطبيق على تلفون المدير، وسجّل دخول باسم المستخدم `admin` (بدون أي إضافات) وكلمة المرور اللي اخترتها.
5. بما إنه أول حساب في النظام، سيصبح تلقائياً **مديراً** وستظهر له كل شاشات الإدارة (قسم الفواتير، إدارة المستخدمين، مراجعة الطلبات).

## 9) إضافة الموظفين

من داخل التطبيق (بحساب المدير فقط):
1. الصفحة الرئيسية → **إدارة المستخدمين وإضافة يوزر جديد**.
2. اضغط **إضافة يوزر جديد**، واملأ: الاسم، اسم المستخدم (بدون مسافات)، كلمة المرور، وحدد الأقسام المسموح له رفع مستندات فيها (مواني/جمارك/أرضيات/إذن).
3. أعطِ الموظف اسم المستخدم وكلمة المرور دي ليسجّل دخول بيهم من تلفونه.

---

### ملاحظات مهمة

- أي تغيير تعمله لاحقاً في صلاحيات موظف (من شاشة إدارة المستخدمين) يُطبّق فوراً في المرة الجاية اللي يفتح فيها التطبيق.
- إيقاف حساب موظف (زر "إيقاف الحساب") يمنعه من الدخول للتطبيق نهائياً دون حذف بياناته أو سجل رفعه السابق.
- لو غيّرت مفتاح Firebase أو أنشأت مشروع Firebase جديد لاحقاً، كل اللي تحتاجه هو تحديث قيمة الـ Secret `GOOGLE_SERVICES_JSON` بمحتوى الملف الجديد، وأعد تشغيل البناء.
